# 编译产物 / Build artifacts (只保留需要的)
*.obj
*.lib
*.log
*.lib
*.so
*.log
*.md
# SCons 构建文件 / SCons build files
.sconsign.dblite

# 其他可执行文件（只保留 nx_main.exe）
test_*.exe

# 忽略 NexusPlugins-main 目录（源代码在子目录中）
NexusPlugins-main

# 允许 docs 目录下的文档文件
docs